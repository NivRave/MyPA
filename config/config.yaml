# MyPA Default Configuration
# Secrets should be set via environment variables (see .env.example)

server:
  gateway_port: 8080
  orchestrator_port: 8081

google:
  client_id: ""
  client_secret: ""
  redirect_url: "http://localhost:8080/oauth/callback"

tavily:
  api_key: ""

gemini:
  model: gemini-2.5-flash
